import { createFromIconfontCN } from '@ant-design/icons';

const Icon = createFromIconfontCN({
  scriptUrl: '//at.alicdn.com/t/font_2199580_h0x55a3z0zi.js', // 在 iconfont.cn 上生成
});

export default Icon;
